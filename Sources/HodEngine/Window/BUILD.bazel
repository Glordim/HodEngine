load("@rules_cc//cc:defs.bzl", "cc_library")

PlatformDefine = "PLATFORM_WINDOWS"

cc_library(
	name = "Window",
	srcs = glob(["**/*.cpp"]),
	hdrs = glob(["**/*.h", "**/*.inl"]),
	deps = ["//Sources/HodEngine/Core:Core"],
	#includes = ["//Sources"],
	defines = [PlatformDefine],
	copts = [
		#"-std=c++17",
		"/std:c++17",
	],
	include_prefix = "Window",
	strip_include_prefix = "Src",
)
