/*
Dummy file
Require by some native build system to use OBJ lib
From Cmake doc :
From Some native build systems (such as Xcode) may not like targets that have only object files,
so consider adding at least one real source file to any target that references $<TARGET_OBJECTS:objlib>.
*/

namespace hod
{
    void HodEngineDummy()
    {
    }
}
